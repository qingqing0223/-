微信公众号数据放这里。每位同学请在本目录下建立自己的日期/节点子目录。

V3目录示例：`2026-09-20_wechatmp01/`，包含表1—表5 CSV、六张工作表的XLSX、
有效记录JSONL和 `summary.json`。表2、表4当前仅保留结构。
`summary.json` 中的采集状态、逐关键词分页统计和两组表输出时间用于判断完整性；
验证码中断或未搜索完的结果不可当成完整轮次。
正文列为公开搜索摘要。未知数据为空。CSV导入Excel时请把ID列设置为文本，推荐直接使用XLSX。
排除记录、逐轮原始数据和浏览器状态保存在被Git忽略的 `data/wechat_mp`，不放入提交目录。
运行说明见 `docs/WECHAT_MP_V3.md`。不要提交任何敏感凭据，不自动commit/push。
